# Add this ABOVE the server block in /etc/nginx/sites-enabled/procncmaroc
# Create the cache directory: mkdir -p /var/run/nginx-cache

fastcgi_cache_path /var/run/nginx-cache levels=1:2 keys_zone=API:10m inactive=60m;

# Then replace the location /api/ block with:

# location /api/ {
#     client_max_body_size 500M;
# 
#     # Only cache GET requests to non-admin endpoints
#     set $no_cache 0;
#     if ($request_method != GET) { set $no_cache 1; }
#     if ($request_uri ~ ^/api/admin/) { set $no_cache 1; }
# 
#     include fastcgi_params;
#     fastcgi_pass unix:/run/php/php8.3-fpm.sock;
#     fastcgi_index index.php;
#     fastcgi_param SCRIPT_FILENAME /var/www/procncmaroc/backend/public/index.php;
#     fastcgi_param REQUEST_URI $uri?$args;
#     fastcgi_param QUERY_STRING $args;
# 
#     fastcgi_cache API;
#     fastcgi_cache_valid 200 5m;
#     fastcgi_cache_methods GET HEAD;
#     fastcgi_cache_use_stale error timeout updating http_500;
#     fastcgi_cache_min_uses 1;
#     fastcgi_cache_lock on;
#     fastcgi_cache_bypass $no_cache;
#     fastcgi_no_cache $no_cache;
#     add_header X-FastCGI-Cache $upstream_cache_status;
# }
